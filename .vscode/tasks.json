{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Copy data",
            "type": "shell",
            "command": "robocopy \"${workspaceFolder}\\bin\" \"${workspaceFolder}\\build\\bin\" /E /IS /IT /MIR /MT64 /NFL /NDL /NJH /NJS /nc /ns /np /xf *.exe /xf *.dll /xf *.pdb /xf *.ilk& if %errorlevel% leq 3 exit /b 0"
        }
    ]
}
